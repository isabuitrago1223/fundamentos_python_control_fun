# Definimos una secuencia de prueba para que el editor no marque error
secuencia = [1, 2, 3, 4, 5]

# Estructura base del bucle for-else
for elemento in secuencia:
    # Cuerpo del bucle
    pass
else:
    # Código que se ejecuta si el bucle termina normalmente
    print("El bucle terminó de recorrer toda la secuencia sin interrupciones.")